package com.sarmad.admobify.adsdk.howtouse

class HowToUseOpenAppAd {

    /**
     *
     *         OpenAppAd().init(
     *             application = application,
     *             adId = getString(R.string.open_app_ad),
     *             remote = true,
     *             preloadAd = false, // if you want to preload ad after initialization set true
     *             loadOnPause = true, // when app is stopped ad will be loaded and show when app is resumed again
     *             reloadOnDismiss = true // Reload open ad on dismiss
     *         )
     */

}